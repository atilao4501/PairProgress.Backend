services:
  pairprogress.backend:
    image: pairprogress.backend
    build:
      context: .
      dockerfile: PairProgress.Backend/Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports: 
      - "8080:8080"
